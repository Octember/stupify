// @ts-check
import { defineConfig } from 'astro/config';

// Static marketing site for stupif.ai — builds to ./dist, deploys to Cloudflare Pages.
export default defineConfig({
  site: 'https://stupif.ai',
});
