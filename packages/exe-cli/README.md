# @stupify/exe-cli

Control-plane helpers for agents provisioned onto exe.dev.

`@stupify/cli` consumes this for the laptop/control-plane side: stable Bun discovery, repo and host validation,
cron wiring, exe.dev integration lookup, VM naming, setup-script generation, and Codex gateway config.
